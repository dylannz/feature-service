version: 1.0

features:

  stripe_billing:
    rules:
      enable:
        - field: "customer_id"
          values:
            eq:
              - "111"
      disable:
        - field: "customer_id"
          values:
            eq:
              - "222"

